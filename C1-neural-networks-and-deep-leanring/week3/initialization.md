- 0 doesn't work, all w is the same values -> neuron is the same
- np.random.randn() * 0.01 -> make w small
- w small so it converges faster (z = wx + b), if z small or large, gradient is very small (~ 0)
